<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">

      <div class="flex-shrink-0">
        <%= link_to root_path, class: "text-gray-900 font-medium hover:text-gray-700" do %>
          Project Tracker
        <% end %>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= link_to 'New Project', new_project_path, class: "px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-md transition-colors" %>
          <%= link_to 'Sign Out', destroy_user_session_path, 
                      method: :delete, 
                      class: "px-4 py-2 text-red-600 hover:bg-red-50 rounded-md transition-colors" %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: "px-4 py-2 text-gray-600 hover:bg-gray-100 rounded-md transition-colors" %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "px-4 py-2 bg-indigo-600 text-white hover:bg-indigo-700 rounded-md shadow-sm transition-colors" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>